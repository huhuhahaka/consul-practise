spring:
  cloud:
    consul:
      host: localhost
      port: 8500
      config:
        enabled: true
        format: yaml
        prefix: my-config
        profile-separator: ','
        data-key: my-data
        watch:
          enabled: true